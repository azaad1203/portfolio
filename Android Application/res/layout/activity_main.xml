<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

   <!-- drawable used to give image a position against text -->
    
    <Button
        android:id="@+id/bt0"
        android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    
    android:drawableBottom="@drawable/gym02"
    android:layout_marginTop="35dp" 	
    android:text="@string/button_gym"
    android:textSize="30sp"
    android:background="@drawable/touch"
    android:onClick="gymList"
						/>

    
    <!-- "android:background" in the button define the style of changing picture depending on either it is pressed or in default stage, put the name of the file that holds pic changing codes -->
   
    <Button
        android:id="@+id/bt1"
        android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:drawableBottom="@drawable/pg01"		
    android:layout_below="@+id/bt0"
    android:text="@string/button_pground"
    android:textSize="30sp"
    android:background="@drawable/touch2"
    android:onClick="pgList"
    />
    
        <Button
        android:id="@+id/bt2"
        android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:drawableBottom="@drawable/data1"		
    android:layout_below="@+id/bt1"
    android:text="@string/button_data"
    android:textSize="30sp"
    android:background="@drawable/touch3"
    android:onClick="data"
    />
    

    
</RelativeLayout>
